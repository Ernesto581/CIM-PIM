PORT=3002
# URL base del proveedor compatible con OpenAI. Cambiar si se usa otro gateway.
LLM_BASE_URL=https://api.openai.com/v1
# Modelo a utilizar (gpt-4o, gpt-3.5-turbo, etc.)
LLM_MODEL=gpt-4o
# Clave de API (nunca subir al repositorio)
LLM_API_KEY=CHANGEME
